# Production browser traffic is routed through the same OCI API Gateway origin.
# The UI container proxies /api and Socket.IO requests to the IAM backend.
VITE_API_URL=.
VITE_BACKEND_URL=.
VITE_WS_URL=.
